class Solution {
public:
    int returnToBoundaryCount(vector<int>& nums) {
        int count = 0;
        int posn = 0;
        for(int i = 0; i<nums.size();i++){
            posn += nums[i];
            if(posn==0){
                count++;
            }
        }
        return count;
    }
};
